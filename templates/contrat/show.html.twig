{% extends 'agent.html.twig' %}

{% block title %}Contrat{% endblock %}

{% block main %}
    <h1>Contrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contrat.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ contrat.type }}</td>
            </tr>
            <tr>
                <th>Date De Depart</th>
                <td>{{ contrat.dateDeDep ? contrat.dateDeDep|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date De Retour</th>
                <td>{{ contrat.dateDeRet ? contrat.dateDeRet|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contrat_index') }}">retour vers liste</a>

    <a href="{{ path('contrat_edit', {'id': contrat.id}) }}">modifier</a>

    {{ include('contrat/_delete_form.html.twig') }}
{% endblock %}
