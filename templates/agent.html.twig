{% extends 'base.html.twig' %} 

{% block body %}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{ path('index') }}">Location Voitures</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
						<a class="nav-link" href="{{ path('client_index') }}">
							clients
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('voiture_index') }}">
							Voitures
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('contrat_index') }}">
							Contrats
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('facture_index') }}">
							Factures
						</a>
					</li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
			<a class="nav-link btn btn-danger my-2 my-sm-0" href="{{ path('app_logout') }}">Déconnexion</a>
    </form>
  </div>
</nav>


<div class="container-fluid">
		<main role="main">
			{% block main %} {% endblock %}
		</main>
</div>
{% endblock %}
